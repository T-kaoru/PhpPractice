<?php
    // 自分の得意な言語で
    // Let's チャレンジ！！
    $a = fgets(STDIN);
    $b = fgets(STDIN);
    $c = fgets(STDIN);

    echo $nage;

    if ( $a === $b ){
        if ( $a === $c ){
            echo "YES";
        }
    }else{
        echo "NO";
    }
?>
